/* 
Write a program that prints its input one word per line
 */

#include <stdio.h>

int main () {

    int c;
    int word;

    while((c == getchar() != EOF)) {
        
        putchar(word)
    }


    printf("%d\n", word);

}